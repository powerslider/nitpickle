{
  "name": "ui-proof-fixture-app",
  "version": "0.0.0",
  "private": true,
  "description": "Fixture app for dogfooding the ui-proof skill. Serves a clean flow and a seeded-defect flow.",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
